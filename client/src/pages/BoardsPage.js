// Contains the main screen that displays all boards and lets users create, rename, delete, and later open a board.

import { useEffect, useState } from "react"

export default function BoardsPage() {
}